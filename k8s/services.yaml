#Wallet Service Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wallet-service
  namespace: swaggies
  labels:
    app: wallet-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: wallet-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: wallet-service
        tier: backend
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3003"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: swaggies-sa
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: wallet-service
          image: us-central1-docker.pkg.dev/swaggies-escrow/swaggies/wallet-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3003
              name: http
            - containerPort: 5003
              name: grpc
          envFrom:
            - configMapRef:
                name: swaggies-config
            - secretRef:
                name: wallet-service-secret
          env:
            - name: PORT
              value: "3003"
            - name: SERVICE_NAME
              value: "wallet-service"
          resources:
            requests:
              cpu: 150m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 20
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 15
            periodSeconds: 15
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: logs
              mountPath: /app/logs
      volumes:
        - name: tmp
          emptyDir: {}
        - name: logs
          emptyDir: {}
      terminationGracePeriodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: wallet-service
  namespace: swaggies
spec:
  type: ClusterIP
  selector:
    app: wallet-service
  ports:
    - name: http
      port: 3003
      targetPort: 3003
    - name: grpc
      port: 5003
      targetPort: 5003
---
#Notification Service Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: swaggies
  labels:
    app: notification-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: notification-service
        tier: backend
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3004"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: swaggies-sa
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: notification-service
          image: us-central1-docker.pkg.dev/swaggies-escrow/swaggies/notification-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3004
              name: http
            - containerPort: 5004
              name: grpc
          envFrom:
            - configMapRef:
                name: swaggies-config
            - secretRef:
                name: notification-service-secret
          env:
            - name: PORT
              value: "3004"
            - name: SERVICE_NAME
              value: "notification-service"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 20
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 15
            periodSeconds: 15
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: logs
              mountPath: /app/logs
      volumes:
        - name: tmp
          emptyDir: {}
        - name: logs
          emptyDir: {}
      terminationGracePeriodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: swaggies
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - name: http
      port: 3004
      targetPort: 3004
    - name: grpc
      port: 5004
      targetPort: 5004
---
#Payment Service Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: swaggies
  labels:
    app: payment-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payment-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: payment-service
        tier: backend
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3005"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: swaggies-sa
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: payment-service
          image: us-central1-docker.pkg.dev/swaggies-escrow/swaggies/payment-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3005
              name: http
          envFrom:
            - configMapRef:
                name: swaggies-config
            - secretRef:
                name: payment-service-secret
          env:
            - name: PORT
              value: "3005"
            - name: SERVICE_NAME
              value: "payment-service"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3005
            initialDelaySeconds: 20
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3005
            initialDelaySeconds: 15
            periodSeconds: 15
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: logs
              mountPath: /app/logs
      volumes:
        - name: tmp
          emptyDir: {}
        - name: logs
          emptyDir: {}
      terminationGracePeriodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: swaggies
spec:
  type: ClusterIP
  selector:
    app: payment-service
  ports:
    - name: http
      port: 3005
      targetPort: 3005
---
#Budget Service Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: budget-service
  namespace: swaggies
  labels:
    app: budget-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: budget-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: budget-service
        tier: backend
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3006"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: swaggies-sa
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: budget-service
          image: us-central1-docker.pkg.dev/swaggies-escrow/swaggies/budget-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3006
              name: http
          envFrom:
            - configMapRef:
                name: swaggies-config
            - secretRef:
                name: budget-service-secret
          env:
            - name: PORT
              value: "3006"
            - name: SERVICE_NAME
              value: "budget-service"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3006
            initialDelaySeconds: 20
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3006
            initialDelaySeconds: 15
            periodSeconds: 15
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: budget-service
  namespace: swaggies
spec:
  type: ClusterIP
  selector:
    app: budget-service
  ports:
    - name: http
      port: 3006
      targetPort: 3006
