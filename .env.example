# ============================================================
# Root .env — Docker Compose shared variables
# Copy this to .env and fill in all values
# ============================================================

NODE_ENV=development
GATEWAY_PORT=3000
GRAFANA_PASSWORD=your_grafana_admin_password
REDIS_PASSWORD=your_redis_password
